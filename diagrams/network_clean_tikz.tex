\documentclass[tikz,border=10pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usepackage{amsfonts}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,shapes.misc,fit,backgrounds,calc}

\begin{document}
\begin{tikzpicture}[
    % Clean node styles with good spacing
    host/.style={circle, draw=blue!60, fill=blue!15, minimum size=6mm, font=\tiny, thick},
    decoy/.style={circle, draw=orange!70, fill=orange!15, minimum size=6mm, font=\tiny, thick, dashed},
    compromised/.style={circle, draw=red!70, fill=red!20, minimum size=6mm, font=\tiny, thick},
    subnet/.style={rounded rectangle, draw=gray!60, fill=gray!8, minimum width=20mm, minimum height=12mm, font=\scriptsize, thick},
    agent/.style={rectangle, draw=black, fill=yellow!15, minimum width=16mm, minimum height=8mm, font=\scriptsize, rounded corners=2pt, thick},
    action/.style={->, thick, blue!70, >=stealth},
    attack/.style={->, thick, red!70, dashed, >=stealth}
]

% Title
\node[font=\large\bfseries] at (0, 4.5) {Cyberwheel Environment};

% Network Layer - well spaced subnets
\node[subnet] (subnet1) at (-2.5, 3) {$N_1$};
\node[subnet] (subnet2) at (0, 3) {$N_2$};  
\node[subnet] (subnet3) at (2.5, 3) {$N_3$};

% Hosts within subnets - cleaner positioning
\node[host] (h1) at (-2.8, 3.3) {$h_1$};
\node[decoy] (d1) at (-2.2, 3.3) {$d_1$};

\node[host] (h2) at (-0.3, 3.3) {$h_2$};
\node[compromised] (h3) at (0.3, 3.3) {$h_3$};

\node[host] (h4) at (2.2, 3.3) {$h_4$};
\node[host] (h5) at (2.8, 3.3) {$h_5$};

% Network backbone
\draw[gray!60, thick] (-2.5, 2.7) -- (0, 2.7) -- (2.5, 2.7);
\node[below, font=\tiny] at (0, 2.6) {Backbone};

% Agents - positioned to avoid overlap
\node[agent] (blue) at (-3, 0.5) {\textbf{Blue}\\Defender};
\node[agent] (red) at (3, 0.5) {\textbf{Red}\\Attacker};

% Action boxes - positioned clearly
\node[rectangle, draw=blue!50, fill=blue!8, text width=18mm, align=left, font=\tiny] at (-3, 1.8) (blue_box) {
    \textbf{Actions:}\\
    Deploy decoy\\
    Remove decoy\\
    Do nothing
};

\node[rectangle, draw=red!50, fill=red!8, text width=18mm, align=left, font=\tiny] at (3, 1.8) (red_box) {
    \textbf{Actions:}\\
    Scan network\\
    Exploit host\\
    Move lateral
};

% Clean action arrows
\draw[action] (blue) -- (blue_box);
\draw[action] (blue_box) to[out=80, in=220] (d1) node[midway, left, font=\tiny, rotate=35] {deploy};

\draw[attack] (red) -- (red_box);
\draw[attack] (red_box) to[out=100, in=320] (h3) node[midway, right, font=\tiny, rotate=-35] {attack};

% State info - bottom boxes
\node[rectangle, draw=blue!50, fill=blue!8, text width=22mm, align=left, font=\tiny] at (-3, -0.5) {
    \textbf{Observes:}\\
    Current alerts\\
    Alert history\\
    Decoy count\\
    $d_b = 2|H| + 2$
};

\node[rectangle, draw=red!50, fill=red!8, text width=22mm, align=left, font=\tiny] at (3, -0.5) {
    \textbf{Knows:}\\
    Host topology\\
    Vulnerabilities\\
    Compromise status\\
    (Fixed policy)
};

% Reward - centered bottom
\node[rectangle, draw=orange!70, fill=orange!10, text width=25mm, align=center, font=\tiny] at (0, -1.2) (reward) {
    \textbf{Rewards}\\
    Blue: +15 deception\\
    Blue: -10 compromise\\
    Red: +attack success
};

\draw[<->, orange!70, thick] (blue) to[out=270, in=180] (reward);
\draw[<->, orange!70, thick] (red) to[out=270, in=0] (reward);

% Legend - right side, well positioned
\node[rectangle, draw=black!60, fill=gray!5, text width=18mm, align=left, font=\tiny] at (5, 1.5) {
    \textbf{Legend}\\
    \node[host, scale=0.6] {}; Clean\\
    \node[decoy, scale=0.6] {}; Decoy\\  
    \node[compromised, scale=0.6] {}; Hacked
};

% Environment info - right side bottom
\node[rectangle, draw=green!60, fill=green!8, text width=18mm, align=left, font=\tiny] at (5, 0.5) {
    \textbf{Network}\\
    7 hosts total\\
    3 subnets\\
    2 decoys active
};

\end{tikzpicture}
\end{document}